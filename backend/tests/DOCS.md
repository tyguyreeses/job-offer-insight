# Backend Tests

API behavior checks against FastAPI routes.

## Key Files

- `conftest.py`: test DB + dependency override
- `test_offers_api.py`: CRUD, validation, compare sorting

## Scope

- `test_offers_api.py` reflects API expectations from `src/main.py` and `src/models.py`.
