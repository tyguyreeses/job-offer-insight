# Alembic

Database migration files.

## Key Files

- `versions/0001_create_offer_table.py`: current baseline schema
- `env.py`: Alembic environment config

## Notes

- Schema migrations are stored in `versions/` and correspond to `backend/src/models.py`.
